---
alwaysApply: true
---
# 🧩 Cursor Project Rules — Flutter + GetX 架构项目

## 🎯 项目目标
- 构建一个 **不联网、轻量、创意类 iOS 应用**（如离线笔记、心情日记、创作工具）。
- 使用 Flutter + GetX 实现干净、模块化的架构。
- 符合 Apple 审核规范（无联网权限、无收集用户数据、UI 优雅美观简洁）。

---

## ⚙️ 架构规范

### 1. 目录结构
本项目基于 **get_cli** 自动生成的 GetX 模块化结构：

lib/
├── app/
│ ├── data/ # 数据层：本地存储、模型、持久化
│ ├── modules/ # 各功能模块（GetX MVC结构）
│ │ ├── home/
│ │ │ ├── controllers/
│ │ │ ├── views/
│ │ │ └── bindings/
│ │ └── ...
│ ├── routes/ # 路由定义
│ ├── services/ # 通用服务，如 ThemeService, StorageService
│ └── widgets/ # 通用小部件
├── main.dart # 入口文件
└── core/ # 全局配置（样式、主题、常量）


---

### 2. 状态管理
- 使用 **GetX** (`get: ^4.7.2`)
- 每个模块包含：
  - `Controller`：业务逻辑
  - `Binding`：依赖注入
  - `View`：UI 页面

Controller 命名规范：`xxx_controller.dart`  
View 命名规范：`xxx_view.dart`

---

### 3. 路由规范
- 全局路由文件：`lib/app/routes/app_pages.dart`
- 命名统一使用 `/模块名`
- 导航统一使用：
  ```dart
  Get.toNamed('/home');
  Get.offNamed('/login');
